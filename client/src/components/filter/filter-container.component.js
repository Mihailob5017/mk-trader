import React from 'react';
import FilterComponent from './filter.component';

const FilterComonentContainer = ({ close }) => {
  return <FilterComponent close={close} />;
};

export default FilterComonentContainer;
